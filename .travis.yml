language: java

jdk: oraclejdk8

before_install:
    - mvn -v
    - cd dependencies
    - mv org ~/.m2/
    - cd -

script:
    - mvn clean test

after_success:
#    - bash <(curl -s https://codecov.io/bash)